/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['movie.naver.com', 'ssl.pstatic.net', 'via.placeholder.com', 'movie-phinf.pstatic.net','blog.kakaocdn.net'],
  },
  reactStrictMode: true,
}

export default nextConfig;